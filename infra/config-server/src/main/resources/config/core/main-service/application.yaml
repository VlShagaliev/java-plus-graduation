server:
  port: 0

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.uuid}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/main
    username: "postgres"
    password: "824322"
    driver-class-name: "org.postgresql.Driver"

stats-server:
  service-id: stats-server

management:
  endpoints:
    web:
      exposure:
        include: health,info