server:
  port: 0

grpc:
  server:
    port: 0

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.uuid}

collector:
  producer-properties:
    bootstrap.servers: localhost:9092
  snapshot-topic: stats.user-actions.v1